import 'package:flutter/material.dart';

class myProtocolo extends StatelessWidget {
  const myProtocolo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
